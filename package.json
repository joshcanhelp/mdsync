{
  "name": "markdown-sync",
  "version": "0.1.0",
  "description": "Sync Markdown files with configurable routing and multi-user support",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "markdown-sync": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest",
    "lint": "tsc --noEmit"
  },
  "keywords": [
    "markdown",
    "sync",
    "notes",
    "multi-user"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@tsconfig/node24": "^24.0.3",
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0",
    "vitest": "^4.0.16"
  },
  "dependencies": {
    "gray-matter": "^4.0.3"
  },
  "engines": {
    "node": ">=18"
  }
}
