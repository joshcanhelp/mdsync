// User-specific configuration file
// Copy this to .markdown-sync.user.js in your home directory or repo root
// DO NOT commit this file - add it to .gitignore

module.exports = {
  // Required: your source directory containing markdown files
  sourceDir: "/Users/username/Documents/Notes",

  // Optional: override auto-detected user ID (from git config user.email)
  // userId: "myusername",

  // Optional: if no repo config exists, you can specify routes here
  // routes: [
  //   {
  //     sourcePath: "Logs/**/*.md",
  //     outputPath: "logs",
  //   },
  //   {
  //     tag: "working",
  //     outputPath: "projects",
  //   },
  // ],

  // Optional: exclude patterns (if no repo config exists)
  // exclude: ["**/private/**"],

  // Optional: required tags (if no repo config exists)
  // requireTags: ["public"],

  // Optional: required props (if no repo config exists)
  // requireProps: {
  //   status: "published",
  // },
};
